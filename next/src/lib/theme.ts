import { createTheme } from '@mui/material/styles';

const theme = createTheme({
  palette: {
    mode: 'dark',
  },
});
export default theme;

export const sideBarWidth = 56;
export const sideBarOpenedWidth = 250;
